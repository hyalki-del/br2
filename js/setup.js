 (function () {     const getStoredTheme = () => {         try {             return localStorage.getItem('babarock_theme') || 'apple';         } catch (e) {             return 'apple';         }     };      const applyTheme = (themeName) => {         const validThemes = ['apple', 'marvel', 'warnerbros'];         const theme = validThemes.includes(themeName) ? themeName : 'apple';         document.documentElement.setAttribute('data-theme', theme);         return theme;     };      applyTheme(getStoredTheme());      window.babarockThemeManager = {         setTheme: (theme) => {             applyTheme(theme);             try {                 localStorage.setItem('babarock_theme', theme);             } catch(e) {}         },         getCurrentTheme: getStoredTheme     }; })();  