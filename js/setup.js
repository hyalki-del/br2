 (function () {     // 1. Initial Theme Application     const applySavedTheme = () => {         const savedTheme = localStorage.getItem('babarock_theme') || 'default';         document.documentElement.setAttribute('data-theme', savedTheme);         return savedTheme;     };      const currentTheme = applySavedTheme();      document.addEventListener("DOMContentLoaded", () => {         // 2. Synchronize Dropdown Selector on setup.html if present         const themeSelector = document.getElementById("themeSelect");         if (themeSelector) {             themeSelector.value = currentTheme;             themeSelector.addEventListener("change", (e) => {                 const selected = e.target.value;                 document.documentElement.setAttribute('data-theme', selected);                 localStorage.setItem('babarock_theme', selected);             });         }     });      // 3. Cross-Tab Theme Synchronization Listener     window.addEventListener('storage', (event) => {         if (event.key === 'babarock_theme') {             const newTheme = event.newValue || 'default';             document.documentElement.setAttribute('data-theme', newTheme);             const themeSelector = document.getElementById("themeSelect");             if (themeSelector) themeSelector.value = newTheme;         }     }); })();  